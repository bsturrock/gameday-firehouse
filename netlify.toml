[build]
  command = "npm install -g pnpm && pnpm install && pnpm --filter @workspace/gameday-firehouse run build"
  publish = "artifacts/gameday-firehouse/dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
